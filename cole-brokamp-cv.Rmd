---
title: "Curriculum Vitae: Cole Brokamp, Ph.D."
output:
  pdf_document: default
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, message = FALSE, warning = FALSE)
```
```{r}
library(dplyr)
library(purrr)
library(glue)
```

# Personal Data

```{r}
#| child: personal_data.Rmd
```

# Research Interests

- Place-based data science
- Environmental epidemiology
- Population health
- Causal inference machine learning methods
- Fairness in precision medicine

# Education

```{r}
#| child: education.Rmd
```

# Academic Appointments

```{r}
#| child: academic_appointments.Rmd
```

# Grants and Contracts

### Current

### Selected Previous (Brokamp, PI)

# Publications

```{r, echo = FALSE, results = 'asis'}
yaml::yaml.load_file("pubs.yaml") |>
  map_chr(\(x) glue_data(x,
                         "{paste(author, collapse = ', ')}",
                         "{title}",
                         "*{journal}*",
                         "{issue_pages}",
                         "{year}.",
                         .sep = ". ",
                         .na = character())) |>
  map_chr(\(x) gsub("Cole Brokamp", "**Cole Brokamp**", x = x, fixed = TRUE)) |>
  cat(sep = "\n\n")
```

# Patents

```{r}
#| child: patents.Rmd
```

# Talks

```{r echo = FALSE, results = 'asis'}
yaml::yaml.load_file("talks.yaml") |>
  map_chr(\(x) glue_data(x,
                         "{title}",
                         "*{event}*",
                         "{location}",
                         "{year}",
                         .sep = ". ",
                         .na = character())) |>
  cat(sep = "\n\n")
```

# Awards and Honors

```{r}
#|child: awards_and_honors.Rmd
```

# Service and Leadership

```{r}
#| child: service_and_leadership.Rmd
```

\vfill

Last Updated: `r Sys.Date()`
