name: 'colebrokamp.com'
output_dir: 'docs'
output:
  html_document:
    css: style.css
    theme: simplex
    highlight: pygments
    include:
      before_body: assets/_google_analytics.html
exclude: ['Makefile', 'README.md', 'renv.lock', 'renv', 'renv/*']
include: ['assets/CV.pdf']
new_session: true
